# Assets

`icon.png`, `adaptive-icon.png`, `favicon.png` and `splash.png` are simple
programmatically-generated brand images (an "anb" wordmark on the green/teal
gradient) so the repository stays self-contained without shipping any
proprietary artwork.

Replace them with your own artwork any time — the filenames are referenced from
`app.json`.
